---
# Run order: provision_ntfy_lxc.yml -> onboard_ntfy_tailscale.yml -> deploy_ntfy.yml
- name: Deploy ntfy push notification service
  hosts: ntfy-lxc-01
  become: true
  vars_files:
    - ../vault.yml
  roles:
    - ntfy
