#This ansible playbook installs wazuh  agent. 
#A smarter man would have written down where it came from. 
#I think it might have come from Wazuh site. 
#
#
- hosts: wazuh_agents
  become: yes
  roles:
    - ../roles/wazuh-ansible
  vars:
    wazuh_managers:
      - address: 192.168.0.11
        port: 1514
        protocol: tcp
        api_port: 55000
        api_proto: 'https'
        api_user: wazuh
        max_retries: 5
        retry_interval: 5

